# Linux Compatibility 事实夹具

这些夹具描述代表环境的事实和预期能力组合，用于 Linux Agent Collector、Adapter 和后续 Compatibility Profile 测试。

- `distribution` 仅记录认证环境，不参与代码分派。
- `expectedCapabilities` 是 Resolver 输入，不等于已完成真实认证。
- 所有夹具当前均为 `fixture_only`，不得展示为 `certified`。
