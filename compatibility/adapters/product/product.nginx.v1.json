{
  "apiVersion": "gcac.adapter/v1",
  "adapterId": "product.nginx",
  "version": "1.0",
  "kind": "product",
  "consumes": ["certificate.material.pem"],
  "produces": ["operation.nginx.certificate.update"],
  "requirements": {
    "id": "product.nginx.requirement",
    "ownerType": "provider_action",
    "ownerId": "product.nginx",
    "requiredAll": [
      { "capabilityKey": "nginx.config_parse", "operator": "equals", "expected": true, "reason": "需要解析 Nginx 证书配置", "riskIfMissing": "无法定位证书绑定" },
      { "capabilityKey": "nginx.cert.install", "operator": "equals", "expected": true, "reason": "需要替换 Nginx 证书材料", "riskIfMissing": "无法完成部署" }
    ],
    "optional": [], "anyOfGroups": [], "forbidden": [], "minConfidence": 80, "allowManual": false, "riskLevel": "critical"
  },
  "conflicts": [], "priority": 100, "riskLevel": "critical", "supportedOperationSchemas": ["1.0"]
}
