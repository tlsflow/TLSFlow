{
  "apiVersion": "gcac.adapter/v1",
  "adapterId": "verifier.tls-remote",
  "version": "1.0",
  "kind": "verifier",
  "consumes": ["expected.certificate.fingerprint"],
  "produces": ["evidence.tls.remote"],
  "requirements": {
    "id": "verifier.tls-remote.requirement", "ownerType": "provider_action", "ownerId": "verifier.tls-remote",
    "requiredAll": [{ "capabilityKey": "certificate.verify", "operator": "equals", "expected": true, "reason": "需要宿主证书验证", "riskIfMissing": "无法确认用户流量使用新证书" }],
    "optional": [], "anyOfGroups": [], "forbidden": [], "minConfidence": 80, "allowManual": false, "riskLevel": "high"
  },
  "conflicts": [], "priority": 100, "riskLevel": "high", "supportedOperationSchemas": ["1.0"]
}
