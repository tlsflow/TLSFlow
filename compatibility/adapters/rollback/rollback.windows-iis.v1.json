{
  "apiVersion": "gcac.adapter/v1",
  "adapterId": "rollback.windows-iis",
  "version": "1.0",
  "kind": "rollback",
  "consumes": ["recovery.ledger"],
  "produces": ["evidence.rollback"],
  "requirements": {
    "id": "rollback.windows-iis.requirement", "ownerType": "provider_action", "ownerId": "rollback.windows-iis",
    "requiredAll": [{ "capabilityKey": "rollback.restore", "operator": "equals", "expected": true, "reason": "需要恢复旧证书绑定", "riskIfMissing": "失败后无法自动恢复" }],
    "optional": [], "anyOfGroups": [], "forbidden": [], "minConfidence": 80, "allowManual": false, "riskLevel": "critical"
  },
  "conflicts": [], "priority": 100, "riskLevel": "critical", "supportedOperationSchemas": ["1.0"]
}
