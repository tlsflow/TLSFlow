ARG POSTGRES_IMAGE=postgres:16-alpine
FROM ${POSTGRES_IMAGE}

LABEL org.opencontainers.image.title="GCAC PostgreSQL"
LABEL org.opencontainers.image.description="GCAC standard database unit; migrations run during Backend startup"
