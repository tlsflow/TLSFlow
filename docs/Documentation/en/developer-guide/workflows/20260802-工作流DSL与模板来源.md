---
title: Workflow DSL and template sources
description: GCAC workflow protocol, template directories, provenance, and versions
docStatus: implemented
productVersion: current
sourceLocale: zh-CN
locale: en-US
specRefs:
  - specs/007-工作流DSL与模板运行管理
  - specs/007.1-工作流DSL、模板版本与来源治理
codeRefs:
  - backend/src/modules/workflow-templates/dto/workflow-templates.dto.ts
  - backend/src/modules/workflow-templates/schema/workflow-templates.schema.ts
  - backend/src/modules/workflow-templates/domain/workflow-templates.domain-service.ts
  - docs/项目规范/20260723-工作流模板管理及编写规范.md
testRefs:
  - backend/src/modules/workflow-templates/workflow-templates.test.ts
  - backend/src/modules/workflow-templates/plugin-workflow-source.test.ts
lastVerified: 2026-08-06
---

# Workflow DSL and template sources

## Protocol boundary

The only workflow protocol is `gcac.workflow/v1`, and the only `kind` is `CurlSshWorkflow`. The root object must contain:

```json
{
  "apiVersion": "gcac.workflow/v1",
  "kind": "CurlSshWorkflow",
  "metadata": {
    "name": "example-certificate-deploy",
    "version": "1.0.0"
  },
  "inputContract": {
    "apiVersion": "gcac.deployment-input/v1",
    "variables": {},
    "connections": {},
    "credentials": {},
    "artifacts": {}
  },
  "steps": []
}
```

`metadata.name` is the stable identifier. `metadata.displayName` is for presentation only. `metadata.version` uses SemVer; the database `WorkflowTemplateVersion.version` is an integer edit-history value and must not be mixed with it.

The Schema rejects unknown root fields, legacy top-level `variables` or `connections`, ordinary `credential` or `certificate` variable types, undeclared references, dangerous JSONata functions, and `foreach` nesting beyond three levels. Templates cannot carry plaintext passwords, tokens, private keys, or production credentials.

## Template sources

| Source | Purpose | Runtime rule |
| --- | --- | --- |
| `backend/src/modules/workflow-templates/builtin-workflows` | Built-in DSL resources shipped with the code | Git-tracked release resources; pages must not scan them into templates at runtime |
| `data/workflows` | User-imported or runtime-retained DSL files | Must not be deleted or rewritten by upgrades; import requires a controlled PluginVersion or explicit migration |

Do not add `docs/工作流样例`, `docs/workflows`, or another third DSL directory. File existence only proves that a resource exists; it does not prove that it is published, enabled, or selectable.

Workflow pages and APIs may select only enabled fixed-version `declarative` PluginVersions with registered Workflow capabilities. `agent_plan` and `isolated_process` plugins cannot be converted into workflows; they enter Agent v2 or Plugin Runner dispatch as a single path.

## Provenance and ownership

- `plugin_internal`: immutable internal WorkflowVersion generated from a PluginVersion; only the plugin can execute it.
- `user`: a user-maintained workflow that can be drafted, published, disabled, and explicitly assigned to a plugin version.
- Historical `plugin_derived` and `legacy` records are handled only by the pre-release manual upgrade ledger; runtime does not read or convert them.

## Publication and hashing

Before publication, validate the DSL, input Contract, Credential and Artifact slots, executors, stages, rollback branches, sensitive paths, and a preview or mock run. Normalize the content and calculate SHA-256. A content change creates a new immutable version; PluginVersion and WorkflowVersion are fixed at plan creation, and execution/retry never resolve legacy sources or `LATEST_PUBLISHED`.
