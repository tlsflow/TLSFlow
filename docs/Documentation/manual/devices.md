---
title: 设备
description: 接入设备、运行发现并确认可部署目标
docStatus: implemented
productVersion: v1.0.0
sourceLocale: zh-CN
locale: zh-CN
specRefs: []
codeRefs:
  - web/src/views/assets/AssetsView.vue
  - backend/src/modules/devices
  - backend/src/modules/agents
testRefs: []
lastVerified: 2026-08-22
---

# 设备资产

设备资产是统一资产的一类，代表证书最终要访问的主机或网络设备；Agent 是设备的管理通道，云服务实例也在统一资产页面中展示，但不伪装为设备。页面入口固定为 `/assets`。

> 【截图占位：设备列表，标出设备名称、管理方式、地址、健康状态、版本和操作按钮】

## 新增并发现设备

1. 进入“资产中心 → 资产”，点击“新增”。
2. 在向导中选择接入平台和管理方式：安装 Agent，或使用页面提供的直接连接方式。
3. 按页面提示填写设备名称、管理地址、端口、TLS 设置和已保存的凭据。
4. 提交向导，等待设备记录创建完成；Agent 接入方式会显示安装或注册材料。
5. 回到设备列表，确认健康状态正常或 Agent 已在线。
6. 打开设备操作菜单，先运行“连接测试”，成功后再运行“发现”。
7. 打开发现结果，逐项确认系统、站点、服务、证书位置和可用能力。
8. 在应用资产向导中只选择状态正常且与应用类型匹配的目标。

连接测试只验证当前地址和登录信息可用；发现才会读取站点、服务和证书位置。发现成功不等于证书已经部署。

## 查看和维护

- 使用分类、管理方式和健康状态筛选设备，快速定位异常设备。
- 点击设备名称查看概览、框架、站点、证书和操作记录。
- 删除设备前确认没有应用资产或部署计划继续使用它。
- 出现“可升级”提示时，先查看当前版本和目标版本，确认维护窗口后再执行升级。

## 常见问题

- **连接测试失败**：检查地址、端口、TLS 校验和凭据，修正后重新测试。
- **发现有警告**：按警告内容补齐站点或证书位置信息，再重新发现。
- **设备在线但不可部署**：查看目标兼容性和能力状态，不能仅依据“在线”判断。
- **Gateway 设备**：Gateway 只负责跨网络转发，仍需要可执行证书操作的目标设备。
