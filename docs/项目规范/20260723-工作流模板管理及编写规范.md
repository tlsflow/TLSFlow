# 工作流模板管理及编写规范

## 适用范围

本文档定义 GCAC 项目当前支持的工作流模板管理规则和工作流 DSL 编写标准。该 DSL 是项目内领域协议，不是行业通用标准。任何生成、保存、校验、执行工作流 DSL 的代码，都必须以本文档和后端 schema 为准。

工作流模板是可被工作流系统加载的 DSL 文件模板，不是独立的 npm 包，也不是另一套数据库实体。本文档同时约束模板的身份、元数据、版本、发布、来源和 DSL 内容。

相关代码位置：

- 后端 DTO：`backend/src/modules/workflow-templates/dto/workflow-templates.dto.ts`
- 后端 schema：`backend/src/modules/workflow-templates/schema/workflow-templates.schema.ts`
- 后端运行时：`backend/src/modules/workflow-templates/domain/workflow-templates.domain-service.ts`
- 后端画布编译器：`backend/src/modules/workflow-templates/domain/workflow-canvas.compiler.ts`
- 前端画布模型：`web/src/views/workflows/workflow-canvas.model.ts`
- 前端画布编辑器：`web/src/views/workflows/WorkflowCanvasEditor.vue`

## 文件模板来源

除数据库中的工作流模板/版本外，系统还支持把内置 DSL 文件和用户导入 DSL 文件识别为“文件模板来源”。

目录约束如下：

- DSL 编辑和模板选择只能使用两个文件来源：内置模板目录与用户导入模板目录。
- 内置模板目录固定为 `backend/src/modules/workflow-templates/builtin-workflows`，该目录下的 DSL 文件必须受 Git 跟踪，适合随系统发布。
- 用户导入模板目录固定为项目根目录下的 `data/workflows`，该目录属于运行数据保存路径，不用于存放系统内置模板。
- 用户 Agent 插件目录固定为项目根目录下的 `data/agent-plugins`，不属于工作流 DSL 文件模板来源。工作流文件模板扫描器不得扫描该目录。
- 禁止新增 `docs/工作流样例`、`docs/workflows` 等第三套 DSL 样例目录；文档中可以说明 DSL 结构，但可执行模板文件必须放入上述两个来源之一。

规则如下：

- 当前只识别 `.json` 和 `.dsl` 文件。
- 文件内容必须是合法的 `gcac.workflow/v1` JSON DSL。
- 扫描结果允许返回无效文件，但无效文件只能展示错误，不能用于创建或覆盖。
- 文件模板 ID 必须携带来源前缀：内置模板为 `builtin/<relativePath>`，用户导入模板为 `user/<relativePath>`。兼容旧调用时，无前缀 ID 按用户目录优先、内置目录兜底解析。
- 文件模板只是“来源”，不是另一套持久化模型。真正的工作流仍然落到数据库的 `workflow_templates` / `workflow_template_versions`。

## 模板身份与元数据

模板身份由 DSL 的 `metadata.name` 决定，必须使用稳定、唯一、全小写的英文标识和短横线命名。发布后不得因为显示名称、Logo、平台或执行方式变化而修改；`metadata.displayName` 只用于界面展示，不作为关联主键。

推荐的模板元数据如下：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | 是 | 稳定模板 ID |
| `displayName` | 否 | 面向用户的名称 |
| `description` | 否 | 模板能力和适用场景说明 |
| `version` | 新模板必填 | SemVer 模板版本，例如 `1.1.0` |
| `logoUrl` | 否 | HTTP(S) URL 或本地 Web 路径 |
| `platforms` | 推荐 | 目标平台或设备标识 |
| `updateMethods` | 推荐 | 更新通道：`ssh`、`curl`，混合模板可同时声明 |
| `maintainer` | 推荐 | 维护团队或责任人 |
| `homepage` | 否 | 文档、发布记录或维护入口 |
| `category` | 否 | 模板分类 |
| `tags` | 否 | 搜索和能力标签 |

`platforms` 和 `updateMethods` 是声明字段，不是权限放行字段。后端仍需在执行前检查资产能力、凭据类型、连接参数和目标平台，不得仅凭模板元数据执行。

## 模板版本与发布

- 新模板从 `1.0.0` 开始；预发布版本必须使用合法 SemVer。
- 修复错误或不改变输入输出契约的调整递进 patch；增加兼容能力、平台或可选步骤递进 minor；改变变量契约、执行语义、必需凭据或回滚行为递进 major。
- 原模板文件不可覆盖后继续使用旧版本号。数据库 `WorkflowTemplateVersion.version` 是工作流编辑历史的整数版本，与模板 SemVer 不同。
- 内置模板必须经过代码评审、Schema 校验、执行计划预览、回滚检查和平台试跑后随版本发布。
- 用户导入模板更新保存到 `data/workflows`，系统不得自动修改已经创建的工作流；用户显式套用模板时，系统才为已有工作流创建新的 draft version。
- 从模板新建工作流后，工作流保留稳定的 `metadata.name`，供模板使用关系展示。

## 模板安全与扩展边界

- 禁止在 DSL 模板中写入明文密码、Token、私钥或生产凭据。
- Logo 和 homepage 只用于展示，不能参与执行、签名或权限判定。本地 Logo 禁止使用 `file://`、`data:` 和包含 `..` 的越界路径。
- 模板失败时必须保留结构化校验错误，不能伪装成可用模板。
- 当前使用 `metadata.name` 关联模板与新建工作流，足够满足单一稳定 ID 的场景。未来如允许别名、分支发布或一个模板生成多个工作流，应新增 `sourcePluginId`、`sourcePluginVersion` 和 `sourcePluginHash`，不得继续依赖工作流名称猜测模板来源。

前端动作约定：

- “从模板新建”：基于文件模板创建新的工作流草稿。
- “套用模板”：把文件模板内容应用到某个已有工作流，生成新的 draft version。

版本规则：

- 套用模板不会修改历史版本。
- 套用模板本质上是创建新的不可变版本。
- 套用模板即使内容与历史版本完全一致，也必须创建新的 draft version，便于从内置模板或用户导入模板刷新当前工作流；普通手动创建草稿版本仍保留重复内容保护。
- 套用到已有工作流时，目标工作流自己的稳定标识 `metadata.name` 必须保留，不能被文件模板的 `metadata.name` 覆盖。
- 文件模板作为插件记录展示时，使用 `metadata.version` 维护插件自身的 SemVer（Semantic Versioning，语义化版本）。
- 新建文件模板必须从 `1.0.0` 或明确的预发布版本开始；每次修改 DSL 模板内容、变量契约、步骤、回滚、Logo、说明或标签时，都必须同步递进 `metadata.version`。
- 只修复向后兼容的问题递进 patch；增加向后兼容能力递进 minor；产生不兼容行为递进 major。禁止更新模板内容但保留原版本号。
- 数据库中的 `WorkflowTemplateVersion.version` 是工作流草稿/发布历史的递增整数，与文件模板插件的 `metadata.version` 是两套不同版本，不得混用。

## 顶层结构

```json
{
  "apiVersion": "gcac.workflow/v1",
  "kind": "CurlSshWorkflow",
  "metadata": {
    "name": "nginx-cert-update",
    "displayName": "Nginx 证书更新",
    "description": "通过 SSH 更新 Nginx 证书文件并验证 HTTPS 服务。",
    "version": "1.0.0",
    "logoUrl": "/plugin-logos/nginx.svg",
    "platforms": ["linux"],
    "updateMethods": ["ssh"],
    "maintainer": "GCAC",
    "homepage": "https://example.com/plugins/nginx-cert-update",
    "category": "certificate",
    "tags": ["nginx", "certificate"]
  },
  "variables": {},
  "steps": [],
  "rollback": []
}
```

字段要求：

- `apiVersion` 必须是 `gcac.workflow/v1`。
- `kind` 必须是 `CurlSshWorkflow`。
- `metadata.name` 必填，必须是稳定英文标识。
- `metadata.displayName` 可选，用于插件市场显示名称。
- `metadata.description` 可选，用于插件卡片和详情说明。
- `metadata.version` 可选以兼容历史模板；新建模板必填，格式必须符合 SemVer，例如 `1.0.0`、`1.2.0-beta.1`。
- `metadata.logoUrl` 可选，支持 `http://`、`https://` 远程 URL，也支持 `/plugin-logos/example.svg`、`plugin-logos/example.svg` 这类前端可访问的本地 Web 路径。
- `metadata.logoUrl` 禁止使用 `file://`、`data:`、任意其他协议或包含 `..` 的越界路径。这里的“本地路径”是浏览器可访问的静态资源路径，不是服务器任意磁盘绝对路径。
- `metadata.platforms` 用于声明插件面向的平台，必须是非空字符串数组。推荐使用稳定的小写标识，例如 `linux`、`windows`、`synology-dsm`、`apache`、`nginx`，禁止把展示文案当平台标识。
- `metadata.updateMethods` 用于声明插件实际使用的更新通道，当前只允许 `ssh`、`curl`。它描述模板怎样连接并修改目标，不应根据标签或步骤名称猜测。
- `metadata.maintainer` 可选，用于标识维护团队或责任人；内置插件推荐统一填写 `GCAC`，用户插件应填写实际维护者。
- `metadata.homepage` 可选，必须是 HTTP(S) URL，用于指向插件说明、发布记录或维护入口。
- `metadata.category` 和 `metadata.tags` 可选，用于插件市场分类、搜索和能力识别。
- `variables` 必填，用于声明运行时变量。
- `steps` 必填，必须是非空数组。
- `rollback` 可选，用于主流程失败后的回滚步骤。

## 插件管理规范

DSL 文件模板在插件市场中是“工作流模板插件发布包”，但不引入第三套持久化模型。内置工作流模板插件仍位于 `backend/src/modules/workflow-templates/builtin-workflows`，用户工作流模板插件仍位于 `data/workflows`，工作流实例和版本仍写入数据库。用户 Agent 插件位于 `data/agent-plugins`，不参与本节工作流文件模板扫描。

### 身份与版本

- `metadata.name` 是插件稳定 ID。发布后不得随展示名称、平台或实现调整而变化。
- `metadata.version` 是插件发布版本，采用 SemVer；数据库工作流整数版本不是插件版本。
- 修改执行步骤、变量契约、回滚逻辑、平台范围、更新方式、Logo、说明或维护信息，都必须递进插件版本。
- 插件文件不得原地覆盖后仍保留旧版本号；否则无法审计某个工作流究竟来自哪次发布。

### 平台与更新通道

- `platforms` 描述适用目标，例如操作系统、设备产品或服务类型。一个插件可以面向多个平台，但不应使用 `all` 掩盖未验证的平台。
- `updateMethods` 描述真实执行通道。包含 SSH/SFTP/SCP 节点的插件声明 `ssh`；包含 HTTP/CURL 节点的插件声明 `curl`；混合流程同时声明两者。
- 平台兼容性必须由模板作者明确维护。运行前仍需根据资产能力、凭据类型和连接方式做后端校验，插件元数据不能替代执行前检查。

### 发布与更新

- 内置插件通过代码评审和系统版本发布；文件、Logo、测试和规范必须在同一变更中提交。
- 用户工作流模板插件通过导入或替换 `data/workflows` 中的 DSL 文件更新；建议保留旧版本文件或外部版本库记录，不应只保存最后一次内容。`data/agent-plugins` 中的 Agent 插件按自身包版本规则管理，不得由工作流模板更新逻辑覆盖。
- 插件更新只影响插件市场中的后续创建操作，不自动改写已经创建的工作流，也不修改历史工作流版本。
- 需要升级现有工作流时，必须由用户显式“套用模板”，创建新的不可变 draft version，再验证和发布。

### 使用状态

- 当前“正在使用”以插件稳定 ID `metadata.name` 与数据库工作流 `name` 的匹配关系判定。
- 从插件创建工作流时保留 `metadata.name`，因此可以在不增加新数据库字段的情况下统计使用状态。
- 如果未来允许一个插件创建多个不同稳定 ID 的工作流，应新增明确的 `sourcePluginId` 和 `sourcePluginVersion` 持久化字段，不能继续依赖名称猜测。

### 安全与质量

- 插件不得包含明文 Secret、私钥或真实生产凭据。
- 远程 Logo 和主页只是展示信息，不参与执行、签名或权限判定。
- 新插件必须至少通过 Schema 校验、执行计划预览、回滚检查和目标平台真实试跑后，才能标记为可发布。

## 导入与覆盖

前端工作流编辑器支持在 DSL 面板中直接导入 DSL JSON，或选择本地 DSL 文件覆盖当前画布草稿。

规则如下：

- 导入入口位于 `web/src/views/workflows/WorkflowCanvasEditor.vue` 的 DSL 面板。
- 导入动作只覆盖浏览器中的当前画布草稿，不会自动保存到后端。
- 导入完成后，必须显式点击“保存草稿”，才会创建新的工作流版本。
- 导入 DSL 时，顶层结构必须满足：
  - `apiVersion = gcac.workflow/v1`
  - `kind = CurlSshWorkflow`
  - `metadata` 为对象
  - `variables` 为对象
  - `steps` 为非空数组
- 当前画布会把 DSL 转成纵向阶段流程图，并自动重新排列节点。

兼容说明：

- 画布当前主要可视化编辑主流程节点、阶段、变量和常用节点字段。
- 对于画布暂未直接暴露的高级字段，例如：
  - `http.request.headers`
  - `retry`
  - `extract`
  - 高级 `assert`
  - 自定义 `rollback`
- 导入时画布会尽量保留这些字段；再次保存或试跑时必须交给后端画布编译器生成正式 DSL，避免前端成为 DSL 事实源。
- 但如果用户在画布中把对应节点类型改掉，或删除原节点，这些透传字段也会随之丢失。这是当前版本的已知边界。

## 阶段规范

每个 step 可以声明 `stage`：

```json
{
  "name": "reload_nginx",
  "type": "ssh",
  "stage": "refresh"
}
```

支持的阶段：

| stage | 中文名 | 用途 |
| --- | --- | --- |
| `prepare` | 准备 | SSH 认证登录、CURL 认证、凭据获取、前置上下文准备 |
| `backup` | 备份 | 保存变更前状态、生成回滚材料 |
| `install` | 安装 | 上传、写入或替换证书/配置/文件 |
| `refresh` | 刷新 | 重载服务、刷新缓存、触发生效 |
| `verify` | 验证 | 检查 HTTP 状态、证书指纹、服务状态、人工验收 |

执行规则：

- 只要 `steps` 中任一 step 声明了 `stage`，后端按固定阶段顺序执行：`prepare` → `backup` → `install` → `refresh` → `verify`。
- 同一阶段内保持原始数组顺序。
- 没有声明 `stage` 的旧模板保持原始数组顺序，保证兼容。
- 新增 DSL 必须显式声明 `stage`。

## 变量规范

变量定义写在 `variables` 中：

```json
{
  "deviceHost": {
    "type": "string",
    "required": true,
    "description": "目标主机"
  },
  "sshUsername": {
    "type": "string",
    "required": true,
    "description": "SSH 用户名"
  },
  "serverCert": {
    "type": "certificate",
    "required": true,
    "sensitive": true,
    "description": "服务端证书产物",
    "artifactContract": {
      "outputs": {
        "certFile": {
          "role": "public_certificate",
          "required": true,
          "format": "pem",
          "encoding": "utf8",
          "description": "服务端证书文件内容"
        },
        "keyFile": {
          "role": "private_key",
          "required": true,
          "format": "pem",
          "encoding": "utf8",
          "description": "服务端私钥文件内容"
        }
      }
    }
  },
  "certificateFilePath": {
    "type": "string",
    "required": true,
    "default": "/etc/ssl/certs/site.pem",
    "description": "服务端证书目的路径"
  },
  "certificateKeyFilePath": {
    "type": "string",
    "required": true,
    "default": "/etc/ssl/private/site.key",
    "description": "服务端私钥目的路径"
  },
  "backupRoot": {
    "type": "string",
    "required": true,
    "default": "/var/backups/gcac-certs",
    "description": "证书备份根目录"
  },
  "frameworkType": {
    "type": "enum",
    "required": true,
    "default": "NGINX",
    "enum": ["NGINX", "APACHE", "TOMCAT", "IIS", "CUSTOM"],
    "description": "应用资产框架类型。"
  },
  "siteName": {
    "type": "string",
    "required": true,
    "description": "应用资产站点显示名称。"
  },
  "bindingInformation": {
    "type": "string",
    "required": false,
    "description": "站点绑定信息，例如 IIS 的 *:443:example.com 或 Web Server 的 listen/server_name 摘要。"
  },
  "hostHeader": {
    "type": "string",
    "required": false,
    "description": "HTTP Host 头，通常等于站点域名。"
  },
  "port": {
    "type": "number",
    "required": true,
    "default": 443,
    "description": "应用资产访问端口。"
  },
  "protocol": {
    "type": "enum",
    "required": true,
    "default": "HTTPS",
    "enum": ["HTTP", "HTTPS", "TLS", "CUSTOM"],
    "description": "应用资产访问协议。"
  },
  "verifyUrl": {
    "type": "string",
    "required": true,
    "description": "后端和工作流共同使用的证书验证 URL。"
  },
  "sniName": {
    "type": "string",
    "required": false,
    "description": "TLS SNI 名称，未配置时默认使用 hostHeader 或 verifyUrl 主机名。"
  },
  "credential": {
    "type": "credential",
    "required": true,
    "sensitive": true,
    "default": {
      "id": "sec_device_login",
      "kind": "username_password",
      "type": "password",
      "username": "root"
    },
    "description": "连接凭据"
  }
}
```

支持的变量类型：

- `string`
- `number`
- `boolean`
- `enum`
- `object`
- `file`
- `credential`
- `certificate`

变量引用格式：

```text
{{deviceHost}}
{{sshUsername}}
{{serverCert.outputs.certFile.content}}
{{certificateFilePath}}
{{previous.extracted.accessToken}}
{{steps.login.output.body.token}}
```

规则：

- 变量名必须是英文标识符：`^[a-zA-Z][a-zA-Z0-9_]*$`。
- 引用变量前必须先在 `variables` 中声明，或由上游 extractor 产出。
- `credential` 类型的 `default` 必须是凭据绑定对象，或留空后在运行时从凭据选择器选择。
- `certificate` 和 `credential` 默认视为敏感变量。
- `certificate` 变量必须优先使用 `artifactContract.outputs` 声明本工作流需要的证书产物文件槽位；后续节点通过 `{{变量名.outputs.槽位名.content}}` 或 `{{变量名.outputs.槽位名.contentBase64}}` 引用真实内容。
- 推荐证书变量命名使用业务语义，例如 `serverCert`、`clientCert`；不要把所有证书都硬编码成 `certificate`。
- `certificate.pem`、`certificate.privateKey` 是早期兼容字段，新模板不得继续使用；新模板必须使用 `outputs`。
- PEM 产物输出项中，`public` 表示叶子证书，`fullchain` 表示叶子证书加证书链且不包含私钥，`chain` 表示单独证书链，`private` 表示私钥，`bundle` 表示格式配置生成的合并 PEM，可能包含私钥。给 Web 服务的 `certFile` 槽位优先绑定 `fullchain`。
- 证书部署目的路径必须在 DSL 中显式声明。表单友好的模板优先使用 `certificateFilePath`、`certificateKeyFilePath`、`backupRoot` 这类标量变量；只有确实需要成组路径结构时才使用 `certificatePaths` 对象。
- 禁止把密码、token、私钥、PFX 明文写进 DSL。

### 应用资产目标变量约定

工作流类型的应用资产可以从 DSL 变量定义中生成资产目标表单，避免资产创建页面和工作流模板重复维护同一组站点信息。证书部署类工作流只要需要绑定到具体站点，就必须优先声明以下变量名；变量名是平台约定，不要在模板中改成同义词。

| 变量名 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| `frameworkType` | `enum` | 是 | 框架类型。枚举固定为 `NGINX`、`APACHE`、`TOMCAT`、`IIS`、`CUSTOM`。 |
| `siteName` | `string` | 是 | 用户可见站点名，用于应用资产列表和部署计划摘要。 |
| `bindingInformation` | `string` | 否 | 站点绑定信息，例如 `*:443:example.com`、`0.0.0.0:8443:api.example.com`。 |
| `hostHeader` | `string` | 否 | HTTP Host 头。Web 站点通常填域名；无 Host 头的 TCP/TLS 场景可以留空。 |
| `port` | `number` | 是 | 应用资产访问端口。 |
| `protocol` | `enum` | 是 | 应用资产访问协议。常用值为 `HTTP`、`HTTPS`、`TLS`、`CUSTOM`。 |
| `verifyUrl` | `string` | TLS 证书部署必填 | 后端探测和工作流验证共用的 URL；HTTPS 站点必须能通过该 URL 拿到远端证书。 |
| `sniName` | `string` | 否 | TLS SNI 名称；未填写时后端可回退到 `hostHeader` 或 `verifyUrl` 主机名。 |

规则：

- `frameworkType/siteName/bindingInformation/hostHeader/port/protocol/verifyUrl/sniName` 是资产目标信息的唯一 DSL 变量来源。新模板不得再新增 `verifyHost`、`verifyPort`、`virtualHostServerName` 等同义变量；旧模板迁移时应保留运行兼容，但表单展示和后端探测以本节变量为准。
- `verifyUrl` 用于平台后端探测，不能只服务于工作流内部 HTTP step。内网、专线或 SNI 特殊场景，应通过 `verifyUrl`、`hostHeader`、`sniName` 明确表达，不要把解析逻辑藏在脚本中。
- `bindingInformation` 是展示和审计字段，不应参与私钥、证书内容或凭据拼接。
- `frameworkType` 使用大写枚举值，与应用资产、站点资产、ManagedTarget 中的 ProviderType 保持一致。
- 如果工作流不面向 Web/TLS 站点，而是设备 API 或自定义部署，仍应声明 `frameworkType = CUSTOM`、`siteName`、`protocol = CUSTOM`；只有无法通过 URL 探测远端证书时，才允许省略 `verifyUrl`。

证书产物合同示例：

```json
{
  "serverCert": {
    "type": "certificate",
    "required": true,
    "sensitive": true,
    "description": "服务器证书产物",
    "artifactContract": {
      "outputs": {
        "certFile": {
          "role": "public_certificate",
          "required": true,
          "format": "pem",
          "encoding": "utf8",
          "description": "写入服务证书文件的内容"
        },
        "keyFile": {
          "role": "private_key",
          "required": true,
          "format": "pem",
          "encoding": "utf8",
          "description": "写入服务私钥文件的内容"
        },
        "chainFile": {
          "role": "certificate_chain",
          "required": false,
          "format": "pem",
          "encoding": "utf8",
          "description": "可选证书链文件内容"
        }
      }
    }
  }
}
```

应用资产保存 `certificateArtifactBindings`，把 DSL 里的证书变量槽位映射到证书格式配置的输出项：

```json
{
  "serverCert": {
    "certificateFormatId": "certfmt_nginx_pem",
    "outputBindings": {
      "certFile": "fullchain",
      "keyFile": "private",
      "chainFile": "chain"
    }
  }
}
```

## 运行时上下文

后端执行时会维护运行时上下文，后续 step 可以引用前面 step 产生的变量和输出。

保留对象：

- `previous`：上一个已执行 step 的快照。
- `steps`：按 step 名称索引的历史 step 快照。

快照结构：

```json
{
  "name": "prepare_auth",
  "type": "http",
  "stage": "prepare",
  "status": "success",
  "output": {
    "statusCode": 200,
    "body": {
      "token": "..."
    }
  },
  "extracted": {
    "accessToken": "..."
  }
}
```

引用规则：

- 优先通过 extractor 提取稳定变量，例如 `accessToken`，后续直接写 `{{accessToken}}`。
- 需要引用紧邻上一步结果时，可以写 `{{previous.extracted.accessToken}}` 或 `{{previous.output.stdout}}`。
- 需要引用指定 step 的结果时，可以写 `{{steps.prepare_auth.extracted.accessToken}}` 或 `{{steps.install_cert.output.body.transferResults[0].hash}}`。
- `sensitive: true` 的 extractor 保存真实值供下游渲染，但 `renderedSteps`、`stepResults`、`stepOutput` 和 `logs` 中必须脱敏展示。
- 执行器必须分离“仅供当前工作流调用链使用的原始响应”和“允许持久化或返回前端的脱敏结果”；禁止把 `[REDACTED]`、`[SECRET_CAPTURED]` 等展示占位符写回运行时变量。
- 正式执行必须在内部 step 状态变化时发布运行进度快照，状态统一使用 `queued`、`running`、`success`、`failed`、`skipped`；禁止等整个工作流结束后一次性补写全部 step 结果。
- `dry-run` 预览必须复用正式执行的逐 step 进度发布机制；即使不发送真实请求或执行真实变更，也必须按 `queued`、`running`、`success`、`failed`、`skipped` 更新前端，不得使用定时动画伪造任务推进。
- `dry-run` 完成后必须返回结构化检查结论。HTTP 节点至少说明已解析的方法、目标地址、响应断言数量和变量提取数量；SSH/文件传输节点至少说明目标主机、操作类型以及未建立真实连接或写入文件的边界。
- 运行进度快照与最终运行结果使用相同的脱敏边界，任何 Token、Secret、私钥或敏感 extractor 原始值都不得进入持久化快照、SSE 事件和前端展示数据。

## 凭据绑定规范

工作流 DSL 不允许用户手写 `secret://...`。凭据通过结构化绑定对象表达：

```json
{
  "credential": {
    "id": "sec_device_login",
    "kind": "username_password",
    "type": "password",
    "username": "root"
  }
}
```

字段规则：

- `id`：Secret 记录编号，例如 `sec_device_login`。
- `kind`：凭据用途，支持 `username_password`、`ssh_key`、`curl_bearer`、`curl_api_key`。
- `type`：Secret 类型，支持 `password`、`ssh_key`、`api_token`。
- `username`：用户名密码或 SSH 私钥凭据的账号名。
- `apiKeyName` / `apiKeyIn`：API Key 的名称和传递位置，`apiKeyIn` 支持 `header` 或 `query`。

执行适配规则：

- DSL 保存 `credential`，不保存 `credentialSecretRef`。
- 后端 `WorkflowTemplatesDomainService` 在运行计划中把 `credential` 转成执行器内部 Secret 引用：
  - `password` -> `secret://password/{id}#current`
  - `ssh_key` -> `secret://ssh_key/{id}#current`

工作流运行时执行器必须通过静态 Executor Registry（执行器注册表）解析，禁止在主执行流程中按产品或平台增加条件分支：

- 当前注册的执行器标识为 `017.CURL_HTTP`、`015.SSH`、`workflow.condition`、`workflow.transform`、`workflow.wait`、`workflow.manual`。
- 同一执行器标识重复注册时，装配必须失败，禁止后注册项静默覆盖已有实现。
- DSL 引用了未注册的执行器时，运行时必须返回 `WORKFLOW_EXECUTOR_NOT_REGISTERED`，禁止返回 planned success（计划成功）或伪造执行成功。
- SSH 与 CURL 的请求、凭据、幂等、提取和断言语义保持不变；Registry 只负责稳定解析执行实现，不改变 DSL step 类型和字段。
  - `api_token` -> `secret://api_token/{id}#current`
- 这个内部引用只提供给 SSH/CURL 执行器，不再作为工作流 DSL 编写内容。

禁止写法：

```json
{
  "password": "plain-text-password",
  "privateKey": "-----BEGIN PRIVATE KEY-----..."
}
```

## HTTP/CURL Step

HTTP step 用于认证、调用接口、提取会话参数、验证状态等。

```json
{
  "name": "prepare_auth",
  "type": "http",
  "stage": "prepare",
  "request": {
    "method": "POST",
    "url": "https://{{deviceHost}}/api/login",
    "auth": {
      "type": "bearer",
      "credential": "{{apiCredential}}"
    },
    "timeoutSeconds": 30
  },
  "extract": [
    {
      "name": "accessToken",
      "type": "firstOf",
      "paths": [
        "$.body.token",
        "$.body.access_token",
        "$.body.data.token"
      ],
      "sensitive": true
    },
    {
      "name": "sessionId",
      "type": "firstOf",
      "paths": [
        "$.headers.x-session-id",
        "$.body.sessionId",
        "$.body.data.session.id"
      ],
      "sensitive": true
    },
    {
      "name": "tenantId",
      "type": "outputPath",
      "path": "$.body.data.tenant.id"
    }
  ],
  "assert": [
    {
      "type": "statusCode",
      "equals": 200
    }
  ]
}
```

`request` 支持：

- `method`: `GET`、`POST`、`PUT`、`PATCH`、`DELETE`
- `url`
- `query`
- `headers`
- `headerRefs`
- `bodyType`: `json`、`form`、`multipart`、`raw`、`none`
- `body`
- `form`
- `formCredentialRefs`
- `multipart`
- `auth`
- `tls`
- `timeoutSeconds`
- `maxResponseBytes`
- `successStatusCodes`
- `failOnNon2xx`

HTTP auth 支持：

- `none`
- `basic`：必须提供 `username` 和 `credential`。`credential` 必须是 `username_password` 凭据或 credential 变量引用。
- `bearer`：必须提供 `credential`，通常使用 `curl_bearer` 凭据。
- `api_key`：必须提供 `credential`、`name` 和 `in`，通常使用 `curl_api_key` 凭据。

表单密文字段规则：

- 普通表单字段写在 `request.form`，只允许字符串、数字和布尔值。
- 密文字段写在 `request.formCredentialRefs`，值必须是 credential 变量引用或结构化 credential 绑定对象。
- 后端运行前把 `formCredentialRefs` 转换为 CURL 执行器内部的 `formSecretRefs`，DSL 文件中禁止保存 `secret://...`。
- 典型用途是设备登录接口需要 `account` / `passwd` 这类 form 字段时：账号可以来自 `{{credential.username}}`，密码必须通过 `formCredentialRefs.passwd` 注入，不能写进 `form`。

示例：

```json
{
  "request": {
    "method": "POST",
    "url": "{{deviceBaseUrl}}/webapi/entry.cgi",
    "bodyType": "form",
    "form": {
      "api": "SYNO.API.Auth",
      "version": 7,
      "method": "login",
      "account": "{{synologyCredential.username}}"
    },
    "formCredentialRefs": {
      "passwd": "{{synologyCredential}}"
    }
  }
}
```

`cookie`、`custom_header`、`mtls` 仍属于执行器内部能力，日常工作流编辑器暂不暴露；如后续要支持，必须先补齐密文保存或凭据绑定模型，不能把明文或用户手写 SecretRef 写进 DSL。

Basic 认证示例：

```json
{
  "auth": {
    "type": "basic",
    "username": "{{sshUsername}}",
    "credential": "{{credential}}"
  }
}
```

同一个 `credential` 变量可以在 SSH 中这样使用：

```json
{
  "connection": {
    "host": "{{deviceHost}}",
    "username": "{{sshUsername}}",
    "credential": "{{credential}}"
  }
}
```

前端编辑器凭据选择器约定：

- `web/src/views/workflows/WorkflowCanvasEditor.vue` 会读取后端 Secret 元数据中的工作流凭据登记，为以下节点提供凭据选择器：
  - 变量面板中的 `credential` 类型变量
  - HTTP `basic`
  - HTTP `bearer`
  - HTTP `api_key`
  - SSH / SFTP / SCP
- 前端界面不允许用户手填 `SecretRef` 字符串。变量、节点属性、运行时试跑都使用“选择凭据”的交互。
- 画布内部允许暂存 `authCredentialId`、`credentialId` 这类 UI 辅助字段，但它们不是 DSL 正式字段，不能写入后端 DSL schema。
- 用户点击保存草稿、单节点模拟运行、单节点真实试跑时，前端只能提交画布草稿；后端 `workflow-canvas.compiler.ts` 必须输出正式 DSL 字段：
  - HTTP Basic -> `request.auth.username` + `request.auth.credential`
  - HTTP Bearer -> `request.auth.credential`
  - HTTP API Key -> `request.auth.credential` + `request.auth.name` + `request.auth.in`
  - SSH/SFTP/SCP -> `connection.credential`
  - `variables.<name>.type = "credential"` -> `variables.<name>.default = { id, kind, type, ... }`
- 后端运行前再把 `credential` 物化为执行器内部 Secret 引用，绝不把密码、token、私钥明文写进 DSL。

## 单节点试跑错误输出

单节点模拟运行和真实试跑必须先由后端把画布编译为正式 DSL，再交给运行接口；不允许在 DSL 中增加调试专用字段。

当真实 SSH、SFTP、SCP 或 HTTP 执行失败时，后端必须把失败作为结构化运行结果返回，不能只向上抛出一条字符串异常：

- `stepResult.status` 必须是 `failed`。
- `stepResult.errorCode` 保存稳定错误码，例如 `SSH_CONNECT_FAILED`、`SSH_AUTH_FAILED`。
- `stepResult.errorMessage` 保存用户可读错误，例如 `SSH 连接失败`。
- `stepOutput.body.detail` 保存排障上下文，例如：
  - `target`：目标地址和端口。
  - `stage`：失败阶段，例如 `connect`、`auth`。
  - `category`：错误分类，例如 `network`、`authentication`。
  - `cause`：底层失败原因。
  - `suggestion`：下一步检查建议。

前端运行态面板必须优先展示上述结构化失败详情，然后再展示原始 `stepOutput` 和日志。这样真实 SSH 试跑失败时，用户能看到是网络、端口、防火墙、SSH 服务端还是认证配置问题，而不是只看到 `SSH 连接失败`。

每次单节点试跑或完整工作流执行都必须生成独立的运行 ID。SSH/CURL 执行适配层用运行 ID、节点名和 attempt 组合生成内部幂等键，DSL 作者不能在工作流文件中手写或复用执行器 `idempotencyKey`。

## Transform Step

Transform step 用于把上游 HTTP/SSH/SFTP/SCP 的结构化输出转换为新的工作流上下文变量。第一版只支持 `engine: "jsonata"`。

典型用途：

- 从列表接口返回的数组中筛选目标对象 ID。
- 把 `certificates[].services` 映射成后续 HTTP form 需要的 JSON 字符串。
- 把多个上游变量合并成后续 step 的输入对象。

列表接口字段名或包裹层可能随设备版本变化时，先用 `firstOf` 从完整 step 输出中提取候选数组，例如依次读取 `$.body.data.certificates`、`$.body.certificates`、`$.body.data.items`、`$.body.items`、`$.body.data`。不要把这种设备差异写成宿主代码里的专用解析逻辑。

示例：

```json
{
  "name": "build_service_bindings",
  "type": "transform",
  "stage": "refresh",
  "transform": {
    "engine": "jsonata",
    "input": {
      "certificateDescription": "{{certificateDescription}}",
      "certificates": "{{synologyCertificatesAfterImport}}",
      "previousCertificateId": "{{previousCertificateId}}",
      "previousCertificateServices": "{{previousCertificateServices}}"
    },
    "outputs": {
      "newCertificateId": {
        "expression": "$filter(certificates, function($c){$c.desc = certificateDescription})[0].id"
      },
      "serviceBindingsJson": {
        "expression": "[$map(previousCertificateServices, function($service){{\"service\":$service,\"old_id\":previousCertificateId,\"id\":newCertificateId}})]",
        "format": "jsonString"
      }
    },
    "timeoutMs": 200,
    "maxInputBytes": 1048576,
    "maxOutputBytes": 1048576
  }
}
```

字段规则：

- `transform.engine` 必须是 `jsonata`。这是第一版唯一启用的转换引擎。
- `transform.input` 可选，必须是 JSON 可序列化结构。字符串值支持 `{{variable}}` 渲染；如果整个字符串就是单个变量引用，会保留变量原始类型，不会强制转成字符串。
- `transform.outputs` 必填，必须是非空对象。对象 key 会成为新的工作流上下文变量名，后续 step 可直接用 `{{newVariable}}` 引用。
- `outputs.<name>.expression` 必填，最大 4096 字符。
- `outputs.<name>.format` 可选：
  - `raw`：默认，保留 JSONata 输出的原始 JSON 值。
  - `jsonString`：把 JSONata 输出序列化为 JSON 字符串，适合 HTTP form 中必须传字符串的字段。
- `outputs.<name>.optional` 可选。未设置或为 `false` 时，表达式输出 `null` / `undefined` 会让 step 失败。
- `outputs.<name>.sensitive` 可选。为 `true` 时输出值进入脱敏路径。
- `timeoutMs` 可选，范围 `1-1000`，默认 `200`。
- `maxInputBytes` 可选，范围 `1-1048576`，默认 `262144`。
- `maxOutputBytes` 可选，范围 `1-1048576`，默认 `262144`。

安全边界：

- JSONata 表达式只接收 `transform.input` 和同一 transform step 内已经生成的前序输出。
- 输出只写入工作流运行上下文，不允许访问文件、网络、环境变量或宿主进程对象。
- 后端不向 JSONata 注册任何自定义 I/O 函数。
- 禁用 `$eval(...)`、`$assert(...)`、`$error(...)`。
- 表达式长度、输入大小、输出大小和执行时长都必须受限。
- JSONata 求值必须在隔离 worker 中执行；超时后终止 worker，不能让复杂表达式卡死主执行线程。

扩展原则：

- 可以在后续版本增加 JMESPath、CEL 等转换引擎，但必须继续通过 `engine` 显式声明。
- 禁止为了某个设备或厂商补宿主专用转换代码。数据转换能力必须沉淀为通用 transform step 或通用表达式引擎能力。
- 如新增转换引擎，必须先补 schema、运行时安全边界、前端编辑器字段和本文档。

## SSH Step

SSH step 用于远端命令、脚本、服务重载、文件操作等。

### Host Key 策略

SSH、SFTP、SCP 的 `connection.hostKeyPolicy` 支持三种策略：

- `trust_on_first_use`：首次连接时信任并记录目标 Host Key。当前前端画布新建节点默认使用该策略，适合单节点真实试跑和内网设备接入。
- `strict`：必须已有 Known Hosts 记录，或显式提供 `expectedHostKeyFingerprint`。适合生产模板固化后执行。
- `manual_approval_required`：发现未知 Host Key 时写入待审批记录并拒绝执行。只有接入 Host Key 审批流程后才能用于真实执行；不能作为前端新建节点默认值。

如果目标主机已经能通过普通 SSH 访问，但真实试跑返回 `HOST_KEY_APPROVAL_REQUIRED`、`HOST_KEY_UNKNOWN` 或 `HOST_KEY_MISMATCH`，说明失败点是主机身份校验，不是网络或密码。处理方式是改用 `trust_on_first_use` 完成首次登记，或填写 `expectedHostKeyFingerprint` 后使用 `strict`。

单命令：

```json
{
  "name": "test_nginx",
  "type": "ssh",
  "stage": "refresh",
  "ssh": {
    "mode": "command",
    "connection": {
      "host": "{{deviceHost}}",
      "username": "{{sshUsername}}",
      "credential": "{{credential}}",
      "hostKeyPolicy": "trust_on_first_use"
    },
    "command": "nginx -t",
    "timeoutSeconds": 60
  }
}
```

多命令：

```json
{
  "name": "reload_nginx",
  "type": "ssh",
  "stage": "refresh",
  "ssh": {
    "mode": "command",
    "connection": {
      "host": "{{deviceHost}}",
      "username": "{{sshUsername}}",
      "credential": "{{credential}}",
      "hostKeyPolicy": "trust_on_first_use"
    },
    "commands": [
      "nginx -t",
      "systemctl reload nginx",
      "systemctl status nginx --no-pager"
    ],
    "timeoutSeconds": 120
  },
  "assert": [
    {
      "type": "regex",
      "pattern": ".*"
    }
  ]
}
```

执行规则：

- 一个 SSH step 对应一次 SSH 会话。
- `commands` 中的命令在同一个 SSH 会话中按数组顺序执行。
- 任一命令失败后停止后续命令。
- 结果会聚合 stdout、stderr，并保留每条命令的执行结果。
- `connection.credential` 必须是凭据绑定对象或 credential 变量引用。

`mode` 支持：

- `command`
- `script`
- `interactive`

当前推荐优先使用 `command` + `commands` 表达多个简单命令。

安全边界：

- 禁止把证书私钥、PFX 密码、token 等敏感内容内联到 `ssh.command`、`ssh.commands` 或 `ssh.script`。
- 需要上传证书或私钥文件时，必须使用正式 `sftp` 或 `scp` step，不能用 `cat <<EOF`、`echo`、`printf` 等 SSH 命令拼接文件内容。
- SSH 命令适合执行 `nginx -t`、`apache2ctl -t`、`systemctl reload ...` 这类远端动作，不适合承载文件传输内容。

前端画布导入规则：

- `ssh.command`、`ssh.commands`、`ssh.script` 都会映射到属性面板中的“命令”文本框。
- `ssh.commands` 导入时会按换行拼接显示，保存时仍按多行命令转回 `commands` 数组。
- `ssh.script` 导入时会完整显示脚本内容，保存时必须继续回写为 `mode: "script"` + `script`，不能降级成空 `command`。
- 导入 DSL 时，节点标题优先使用 step 的 `name`，避免复杂模板在画布上全部显示成泛化的“SSH 命令”。

## SFTP/SCP Step

SFTP/SCP step 用于正式文件传输。证书、公钥、私钥、配置片段等文件上传必须放在 `install` 阶段。

SFTP 上传证书：

```json
{
  "name": "install_certificate_pem",
  "type": "sftp",
  "stage": "install",
  "sftp": {
    "direction": "upload",
    "connection": {
      "host": "{{deviceHost}}",
      "username": "{{sshUsername}}",
      "credential": "{{credential}}",
      "hostKeyPolicy": "trust_on_first_use"
    },
    "remotePath": "/etc/nginx/cert.pem",
    "contentRef": "{{serverCert.outputs.certFile.content}}",
    "mode": "0644",
    "timeoutSeconds": 60
  },
  "extract": [
    {
      "name": "certificateUploadHash",
      "type": "jsonPath",
      "path": "$.transferResults[0].hash",
      "optional": true
    }
  ]
}
```

SCP 上传私钥：

```json
{
  "name": "install_certificate_key",
  "type": "scp",
  "stage": "install",
  "scp": {
    "direction": "upload",
    "connection": {
      "host": "{{deviceHost}}",
      "username": "{{sshUsername}}",
      "credential": "{{credential}}",
      "hostKeyPolicy": "trust_on_first_use"
    },
    "remotePath": "/etc/nginx/key.pem",
    "contentRef": "{{serverCert.outputs.keyFile.content}}",
    "mode": "0600",
    "timeoutSeconds": 60
  }
}
```

`sftp` / `scp` 字段支持：

- `direction`: `upload` 或 `download`，必填。
- `connection`: SSH 连接对象，字段与 SSH step 的 `ssh.connection` 一致，必填。
- `remotePath`: 远端路径，必填。
- `contentRef`: 上传内容引用，新模板必须写 `{{serverCert.outputs.certFile.content}}`、`{{serverCert.outputs.keyFile.content}}` 等证书产物槽位表达式。
- `contentEncoding`: `utf8` 或 `base64`，默认按 UTF-8 文本处理。
- `localPath`: 本地路径或虚拟产物路径；上传时可与 `contentRef` 二选一，下载时必填。
- `temporaryPath`: 临时上传路径，用于原子替换。
- `expectedHash`: 期望 SHA-256 hex。
- `expectedSize`: 期望字节数，必须是非负整数。
- `verifyHash`: 是否校验传输后 hash。
- `mode`: 远端文件权限，例如 `0644`、`0600`。
- `owner` / `group`: 远端 owner/group。
- `timeoutSeconds`: 正整数超时秒数。

校验规则：

- `upload` 至少必须提供 `contentRef` 或 `localPath`。
- `download` 必须提供 `localPath`。
- `connection.credential` 必须是凭据绑定对象或 credential 变量引用。
- 文件传输通道允许承载 `serverCert.outputs.keyFile.content` 等敏感内容，但运行计划、日志和返回 detail 必须脱敏展示。
- 如果 SFTP 可用，优先使用 `sftp`；只有明确需要 SCP 协议或目标不支持 SFTP 时，才使用 `scp`。
- 使用 `temporaryPath` 上传时，执行器先写入临时文件并校验，再通过远端 rename 替换目标文件。若目标文件已存在且目标 SFTP 服务不支持 rename 覆盖，执行器允许删除旧目标后重试 rename，并在 `transferResults[].auditDetails` 中记录 `rename_overwrite_fallback`。该降级路径不具备原子性，因此生产模板仍应优先配合 backup step 使用。

## Condition Step

Condition step 用于判断变量或上游输出。

```json
{
  "name": "is_linux",
  "type": "condition",
  "stage": "prepare",
  "condition": {
    "variable": "deviceOs",
    "equals": "linux"
  },
  "description": "只允许 Linux 目标继续"
}
```

支持的判断操作：

- `equals`
- `notEquals`
- `exists`

规则：

- `variable` 必填。
- `equals`、`notEquals`、`exists` 必须且只能出现一个。

## Wait Step

```json
{
  "name": "wait_apply",
  "type": "wait",
  "stage": "refresh",
  "seconds": 10
}
```

## Manual Step

```json
{
  "name": "manual_verify",
  "type": "manual",
  "stage": "verify",
  "instruction": "请人工确认目标服务证书已更新。"
}
```

## Extractor 规范

Extractor 用于从 step 输出中提取变量。`name` 就是后续节点引用的变量名，因此可以把任意响应字段映射成任意运行时变量，不限于 token。

数组写法：

```json
{
  "extract": [
    {
      "name": "remoteFingerprint",
      "type": "jsonPath",
      "path": "$.fingerprint"
    }
  ]
}
```

对象写法：

```json
{
  "extract": {
    "remoteFingerprint": {
      "type": "jsonPath",
      "path": "$.fingerprint"
    }
  }
}
```

支持类型：

- `jsonPath`
- `outputPath`
- `firstOf`
- `header`
- `regex`
- `statusCode`
- `textContains`

提取出的变量可在后续 step 中通过 `{{remoteFingerprint}}` 引用。

提取范围：

- `jsonPath` 从 `output.body` 读取，适合 HTTP JSON body 或 SFTP/SCP 的 `body.transferResults`。
- `outputPath` 从完整 step 输出读取，路径以 `$.` 开头，例如 `$.body.token`、`$.stdout`、`$.body.transferResults[0].hash`。
- `firstOf` 从完整 step 输出按 `paths` 顺序读取第一个非空值，适合不同设备/API 返回字段名不一致的认证场景。
- `header` 从 HTTP 响应 header 读取。
- `regex` 和 `textContains` 从 `stdout` 或响应 body 文本读取。
- `statusCode` 读取 HTTP 状态码。

示例：

```json
{
  "name": "prepare_auth",
  "type": "http",
  "stage": "prepare",
  "request": {
    "method": "POST",
    "url": "https://{{deviceHost}}/api/login"
  },
  "extract": [
    {
      "name": "accessToken",
      "type": "firstOf",
      "paths": [
        "$.body.token",
        "$.body.access_token",
        "$.body.data.token",
        "$.body.result.auth.token"
      ],
      "sensitive": true
    }
  ]
}
```

`firstOf` 规则：

- `paths` 必须是非空数组。
- 每个路径都从完整 step 输出读取，而不是只读 body。
- 按顺序返回第一个不是 `undefined`、`null`、空字符串的值。
- 如果所有候选路径都没有值，且 extractor 没有声明 `optional: true`，该 step 失败。
- 不允许靠系统自动猜字段；字段映射必须是模板的一部分。
- 一个 step 可以声明多个 extractor，例如同时提取 `accessToken`、`sessionId`、`csrfToken`、`tenantId`、`expiresAt`，后续分别通过 `{{accessToken}}`、`{{sessionId}}`、`{{tenantId}}` 引用。

## Assert 规范

Assert 用于判断 step 是否成功。

```json
{
  "assert": [
    {
      "type": "statusCode",
      "equals": 200
    },
    {
      "type": "certificateFingerprint",
      "actual": "{{remoteFingerprint}}",
      "expected": "{{serverCert.fingerprintSha256}}"
    }
  ]
}
```

支持类型：

- `statusCode`
- `jsonPath`
- `header`
- `contains`
- `regex`
- `certificateFingerprint`

`contains` 会先渲染 `{{变量}}`，再对 `stdout` 或响应 body 文本做包含判断；为避免 HTML 文案大小写差异造成误判，比较时忽略大小写。需要严格大小写或复杂文本规则时，必须使用 `regex`。

## Rollback 规范

`rollback` 是失败后执行的回滚步骤。

```json
{
  "rollback": [
    {
      "name": "restore_old_cert",
      "type": "ssh",
      "stage": "install",
      "ssh": {
        "mode": "command",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "commands": [
          "cp /etc/nginx/cert.pem.bak /etc/nginx/cert.pem",
          "nginx -t",
          "systemctl reload nginx"
        ],
        "timeoutSeconds": 120
      }
    }
  ]
}
```

要求：

- 回滚步骤也必须避免明文 Secret。
- 回滚步骤应尽量可幂等。
- 回滚步骤应优先使用已有备份材料。

## 完整示例

```json
{
  "apiVersion": "gcac.workflow/v1",
  "kind": "CurlSshWorkflow",
  "metadata": {
    "name": "nginx-cert-update",
    "displayName": "Nginx 证书更新",
    "category": "certificate",
    "tags": ["nginx", "certificate"]
  },
  "variables": {
    "deviceHost": {
      "type": "string",
      "required": true,
      "description": "目标主机"
    },
    "sshUsername": {
      "type": "string",
      "required": true,
      "description": "SSH 用户名"
    },
    "serverCert": {
      "type": "certificate",
      "required": true,
      "sensitive": true,
      "description": "服务端证书产物",
      "artifactContract": {
        "outputs": {
          "certFile": {
            "role": "public_certificate",
            "required": true,
            "format": "pem",
            "encoding": "utf8",
            "description": "写入服务证书文件的内容"
          },
          "keyFile": {
            "role": "private_key",
            "required": true,
            "format": "pem",
            "encoding": "utf8",
            "description": "写入服务私钥文件的内容"
          }
        }
      }
    },
    "certificateFilePath": {
      "type": "string",
      "required": true,
      "default": "/etc/nginx/cert.pem",
      "description": "服务端证书目的路径"
    },
    "certificateKeyFilePath": {
      "type": "string",
      "required": true,
      "default": "/etc/nginx/key.pem",
      "description": "服务端私钥目的路径"
    },
    "backupRoot": {
      "type": "string",
      "required": true,
      "default": "/var/backups/gcac-certs",
      "description": "证书备份根目录"
    },
    "credential": {
      "type": "credential",
      "required": true,
      "sensitive": true,
      "description": "SSH 连接凭据"
    },
    "apiCredential": {
      "type": "credential",
      "required": true,
      "sensitive": true,
      "description": "准备阶段 API 凭据"
    },
    "frameworkType": {
      "type": "enum",
      "required": true,
      "default": "NGINX",
      "enum": ["NGINX", "APACHE", "TOMCAT", "IIS", "CUSTOM"],
      "description": "应用资产框架类型"
    },
    "siteName": {
      "type": "string",
      "required": true,
      "default": "Nginx HTTPS Site",
      "description": "应用资产站点显示名称"
    },
    "bindingInformation": {
      "type": "string",
      "required": false,
      "default": "*:443:example.com",
      "description": "站点绑定信息"
    },
    "hostHeader": {
      "type": "string",
      "required": false,
      "default": "example.com",
      "description": "HTTP Host 头"
    },
    "port": {
      "type": "number",
      "required": true,
      "default": 443,
      "description": "应用资产访问端口"
    },
    "protocol": {
      "type": "enum",
      "required": true,
      "default": "HTTPS",
      "enum": ["HTTP", "HTTPS", "TLS", "CUSTOM"],
      "description": "应用资产访问协议"
    },
    "verifyUrl": {
      "type": "string",
      "required": true,
      "description": "验证 URL"
    },
    "sniName": {
      "type": "string",
      "required": false,
      "default": "example.com",
      "description": "TLS SNI 名称"
    }
  },
  "steps": [
    {
      "name": "prepare_auth",
      "type": "http",
      "stage": "prepare",
      "request": {
        "method": "POST",
        "url": "https://{{deviceHost}}/api/login",
        "auth": {
          "type": "bearer",
          "credential": "{{apiCredential}}"
        },
        "timeoutSeconds": 30
      },
      "extract": [
        {
          "name": "accessToken",
          "type": "firstOf",
          "paths": [
            "$.body.token",
            "$.body.access_token",
            "$.body.data.token"
          ],
          "sensitive": true,
          "optional": true
        }
      ],
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        }
      ]
    },
    {
      "name": "backup_cert",
      "type": "ssh",
      "stage": "backup",
      "ssh": {
        "mode": "command",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "commands": [
          "stamp=$(date +%Y%m%d_%H%M)",
          "backup_dir={{backupRoot}}/cert_bak_${stamp}",
          "mkdir -p ${backup_dir}",
          "cp -f {{certificateFilePath}} ${backup_dir}/$(basename {{certificateFilePath}})",
          "cp -f {{certificateKeyFilePath}} ${backup_dir}/$(basename {{certificateKeyFilePath}})"
        ],
        "timeoutSeconds": 60
      }
    },
    {
      "name": "install_cert",
      "type": "sftp",
      "stage": "install",
      "sftp": {
        "direction": "upload",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "remotePath": "{{certificateFilePath}}",
        "temporaryPath": "{{certificateFilePath}}.gcac.tmp",
        "contentRef": "{{serverCert.outputs.certFile.content}}",
        "mode": "0644",
        "timeoutSeconds": 60
      }
    },
    {
      "name": "install_key",
      "type": "scp",
      "stage": "install",
      "scp": {
        "direction": "upload",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "remotePath": "{{certificateKeyFilePath}}",
        "temporaryPath": "{{certificateKeyFilePath}}.gcac.tmp",
        "contentRef": "{{serverCert.outputs.keyFile.content}}",
        "mode": "0600",
        "timeoutSeconds": 60
      }
    },
    {
      "name": "reload_nginx",
      "type": "ssh",
      "stage": "refresh",
      "ssh": {
        "mode": "command",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "commands": [
          "nginx -t",
          "systemctl reload nginx"
        ],
        "timeoutSeconds": 120
      },
      "assert": [
        {
          "type": "regex",
          "pattern": ".*"
        }
      ]
    },
    {
      "name": "verify_http",
      "type": "http",
      "stage": "verify",
      "request": {
        "method": "GET",
        "url": "{{verifyUrl}}",
        "timeoutSeconds": 30
      },
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        }
      ]
    }
  ],
  "rollback": [
    {
      "name": "restore_cert",
      "type": "ssh",
      "stage": "install",
      "ssh": {
        "mode": "command",
        "connection": {
          "host": "{{deviceHost}}",
          "username": "{{sshUsername}}",
          "credential": "{{credential}}",
          "hostKeyPolicy": "trust_on_first_use"
        },
        "commands": [
          "echo rollback requires backup certificate files captured by the backup step",
          "nginx -t",
          "systemctl reload nginx"
        ],
        "timeoutSeconds": 120
      }
    }
  ]
}
```

## CURL 证书更换示例

CURL 型设备通常不能直接接触证书文件系统，备份阶段可以跳过或调用设备提供的导出接口。准备阶段获取的认证产物必须通过 extractor 保存为运行时变量，后续 HTTP step 直接引用这些变量。变量名由 DSL 定义，不限于 `accessToken`。

```json
{
  "apiVersion": "gcac.workflow/v1",
  "kind": "CurlSshWorkflow",
  "metadata": {
    "name": "gateway-cert-update",
    "displayName": "网关证书更新",
    "category": "certificate"
  },
  "variables": {
    "deviceHost": {
      "type": "string",
      "required": true,
      "description": "目标网关"
    },
    "apiCredential": {
      "type": "credential",
      "required": true,
      "sensitive": true,
      "description": "API 用户名密码、Bearer Token 或 API Key 凭据"
    },
    "serverCert": {
      "type": "certificate",
      "required": true,
      "sensitive": true,
      "description": "服务端证书产物",
      "artifactContract": {
        "outputs": {
          "certFile": {
            "role": "public_certificate",
            "required": true,
            "format": "pem",
            "encoding": "utf8",
            "description": "提交到设备 API 的证书文件内容"
          },
          "keyFile": {
            "role": "private_key",
            "required": true,
            "format": "pem",
            "encoding": "utf8",
            "description": "提交到设备 API 的私钥文件内容"
          }
        }
      }
    },
    "frameworkType": {
      "type": "enum",
      "required": true,
      "default": "CUSTOM",
      "enum": ["NGINX", "APACHE", "TOMCAT", "IIS", "CUSTOM"],
      "description": "应用资产框架类型"
    },
    "siteName": {
      "type": "string",
      "required": true,
      "default": "Gateway HTTPS Endpoint",
      "description": "应用资产站点显示名称"
    },
    "bindingInformation": {
      "type": "string",
      "required": false,
      "default": "*:443:gateway.example.com",
      "description": "站点绑定信息"
    },
    "hostHeader": {
      "type": "string",
      "required": false,
      "default": "gateway.example.com",
      "description": "HTTP Host 头"
    },
    "port": {
      "type": "number",
      "required": true,
      "default": 443,
      "description": "应用资产访问端口"
    },
    "protocol": {
      "type": "enum",
      "required": true,
      "default": "HTTPS",
      "enum": ["HTTP", "HTTPS", "TLS", "CUSTOM"],
      "description": "应用资产访问协议"
    },
    "verifyUrl": {
      "type": "string",
      "required": true,
      "description": "验证 URL"
    },
    "sniName": {
      "type": "string",
      "required": false,
      "default": "gateway.example.com",
      "description": "TLS SNI 名称"
    }
  },
  "steps": [
    {
      "name": "prepare_auth",
      "type": "http",
      "stage": "prepare",
      "request": {
        "method": "POST",
        "url": "https://{{deviceHost}}/api/login",
        "auth": {
          "type": "basic",
          "username": "{{apiCredential.username}}",
          "credential": "{{apiCredential}}"
        },
        "timeoutSeconds": 30
      },
      "extract": [
        {
          "name": "accessToken",
          "type": "firstOf",
          "paths": [
            "$.body.token",
            "$.body.access_token",
            "$.body.data.token"
          ],
          "sensitive": true
        },
        {
          "name": "sessionId",
          "type": "firstOf",
          "paths": [
            "$.headers.x-session-id",
            "$.body.sessionId",
            "$.body.data.session.id"
          ],
          "sensitive": true
        }
      ],
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        }
      ]
    },
    {
      "name": "install_certificate",
      "type": "http",
      "stage": "install",
      "request": {
        "method": "PUT",
        "url": "https://{{deviceHost}}/api/certificate",
        "headers": {
          "Authorization": "Bearer {{accessToken}}",
          "X-Session-Id": "{{sessionId}}"
        },
        "body": {
          "certificate": "{{serverCert.outputs.certFile.content}}",
          "privateKey": "{{serverCert.outputs.keyFile.content}}"
        },
        "timeoutSeconds": 60
      },
      "extract": [
        {
          "name": "remoteFingerprint",
          "type": "outputPath",
          "path": "$.body.fingerprint"
        }
      ],
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        },
        {
          "type": "certificateFingerprint",
          "actual": "{{remoteFingerprint}}",
          "expected": "{{serverCert.fingerprintSha256}}"
        }
      ]
    },
    {
      "name": "refresh_service",
      "type": "http",
      "stage": "refresh",
      "request": {
        "method": "POST",
        "url": "https://{{deviceHost}}/api/service/reload",
        "headers": {
          "Authorization": "Bearer {{accessToken}}",
          "X-Session-Id": "{{sessionId}}"
        },
        "timeoutSeconds": 60
      },
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        }
      ]
    },
    {
      "name": "verify_certificate",
      "type": "http",
      "stage": "verify",
      "request": {
        "method": "GET",
        "url": "{{verifyUrl}}",
        "timeoutSeconds": 30
      },
      "assert": [
        {
          "type": "statusCode",
          "equals": 200
        }
      ]
    }
  ]
}
```

## 禁止事项

- 禁止把密码、token、私钥、PFX 密码明文写入 DSL。
- 禁止把证书或私钥内容塞进 `ssh.command` / `ssh.commands` / `ssh.script`；上传文件必须使用 `sftp` 或 `scp` step。
- 禁止引用未声明变量。
- 禁止新增 DSL 时省略 `stage`。
- 禁止把多个业务动作塞进一个不可读的大脚本，除非必须保持远端事务性。
- 禁止在前端生成、保存或试跑正式 DSL；前端只能编辑画布，正式 DSL 必须由后端画布编译器生成并校验。
- 禁止修改后端 schema 后不更新本文档。

## 变更同步要求

修改以下内容时必须同步更新本文档：

- `WorkflowDslV1`
- `WorkflowStep`
- `WorkflowVariableDefinition`
- step 类型、字段、校验规则
- stage 阶段定义或执行顺序
- 凭据绑定规则
- SSH/CURL 执行器适配规则
- 后端画布到 DSL 的转换规则

## 变量配置契约与连接槽位

工作流变量必须区分配置阶段和执行阶段。新 DSL 应为每个变量声明以下字段：

```json
{
  "type": "string",
  "configurationMode": "required",
  "source": { "kind": "asset_ssl", "path": "target.verifyUrl" },
  "lifecycle": "pre_execution",
  "bindingPolicy": "fixed",
  "ui": { "group": "basic", "order": 10 }
}
```

- `required`：执行前必须有值；只有需要用户绑定的来源才显示在基础配置区。
- `advanced`：由资产、DSL 默认值或受控派生值提供，默认折叠；只有 `default_overridable` 才允许用户覆盖。
- `runtime`：由证书产物、系统上下文或步骤输出在执行期间注入，配置页面不得渲染，也不得写入资产绑定。
- `source` 描述值的唯一来源；前端不得依据变量名、类型或默认值推断来源。
- `source.kind=asset_ssl` 表示字段由应用资产的“工作流目标信息”托管；`source.kind=derived` 表示字段由受控解析器生成。两者均不得再次进入通用工作流变量列表，也不得被历史 `variableBindings` 覆盖。
- `lifecycle` 描述值何时可用；`runtime_injected` 和 `step_output` 必须配合 `configurationMode=runtime`。
- `bindingPolicy` 描述用户是否可以覆盖值；`required_binding` 用于必须手动绑定的基础配置，`default_overridable` 用于允许修改默认值的进阶配置，`fixed` 可用于任何由资产、派生值、系统或步骤产物唯一决定的变量。

应用资产通用目标字段必须直接引用 `target` 上下文，不得在应用资产页面重复要求用户填写：

```json
{
  "port": {
    "type": "number",
    "configurationMode": "required",
    "source": { "kind": "asset_ssl", "path": "target.port" },
    "lifecycle": "pre_execution",
    "bindingPolicy": "fixed"
  },
  "deviceBaseUrl": {
    "type": "string",
    "configurationMode": "advanced",
    "source": { "kind": "derived", "resolver": "endpoint_url" },
    "lifecycle": "pre_execution",
    "bindingPolicy": "fixed"
  }
}
```

步骤输出必须显式声明为运行时变量。例如 `newCertificateId`、`previousCertificateId`、`serviceBindingsJson` 应使用 `configurationMode=runtime`、`source.kind=step_output`、`lifecycle=step_output` 和 `bindingPolicy=fixed`，不得让用户在配置阶段手工输入。

SSH、SFTP、SCP 和 HTTP 节点可引用命名连接槽位：

```json
{
  "connections": {
    "targetSsh": {
      "protocol": "ssh",
      "host": { "configurationMode": "required", "source": "binding" },
      "port": { "configurationMode": "advanced", "source": "dsl_default", "default": 22 },
      "username": { "configurationMode": "required", "source": "binding" },
      "credential": { "slot": "sshCredential", "configurationMode": "required", "source": "credential" },
      "hostKey": { "configurationMode": "advanced", "policy": "trust_on_first_use" }
    }
  }
}
```

节点通过 `connectionRef` 引用连接槽位。连接槽位的 `host` 来源于工作流资产绑定，不得从 Agent Host、网关或控制面节点名称推断；未声明端口时，SSH 连接解析默认使用 `22`，但投影必须明确展示该默认值。厂商特有字段（例如 Synology 证书描述、API 路径、Nginx/Apache 目标路径）应作为 DSL 变量或兼容性 Profile 的结构化字段定义，不得退化成资产页按变量名猜测的通用输入。

应用资产保存连接槽位时使用 `workflow.connectionBindings`。每个连接绑定至少可包含 `host`、`port`、`username`、`credentialRef`、`credential` 和 `expectedHostKeyFingerprint`：

- `credentialRef` 用于稳定标识用户选择的 Secret，便于编辑页面回显和兼容旧数据。
- `credential` 保存工作流运行时需要的结构化凭据元数据，只允许 `id`、`kind`、`type`、`username`、`apiKeyName`、`apiKeyIn`，禁止保存明文密码、私钥或 Token。
- 部署计划必须同时传递 `connectionBindings`、`parameterBindings` 和兼容用 `variableBindings`；执行器以 `parameterBindings` 覆盖同名旧变量，但不得删除旧绑定导致历史版本失效。
- 运行时必须在统一入口把 `connectionRef` 解析为 SSH 执行器连接，SSH、SFTP、SCP 和 rollback 使用同一规则；禁止各 step 类型自行猜测连接字段。
- 基础配置展示 `host`、`username` 和 `credential`；`port`、Host Key 策略及指纹默认进入高级设置。字段是否折叠只由连接定义中的 `configurationMode` 决定，不能按模板名称硬编码。

旧 DSL 在未使用上述契约字段时继续走基础兼容分类；不得新增按变量名猜测资产字段或步骤产物的兼容补丁。需要优化的内置模板必须新建版本并补齐显式契约；一旦使用任一契约字段，发布校验要求所有变量和连接字段完整声明。

## Agent 原子操作插件边界

Agent 原子操作插件不是工作流 DSL 的新 step 类型，也不得塞入工作流模板目录。两者共享“变量、SecretRef、证书 Artifact 和版本固定”的配置思想，但执行协议彼此独立：

- 工作流 DSL 继续使用 `backend/src/modules/workflow-templates/builtin-workflows` 和 `data/workflows` 两类来源。用户 Agent 插件独立使用 `data/agent-plugins`，不属于工作流 DSL 来源。
- Agent 插件使用 `gcac.agent-plugin/v1` Manifest，由插件目录和应用资产 `AGENT/PLUGIN` 策略管理；历史 `mountId` 仅用于兼容旧资产。
- 工作流 DSL 文件存在于正式目录只表示“已发现”，不表示“可选择”。内置和用户 DSL 模板插件默认禁用，只有租户管理员在插件管理页手动启用后，才进入工作流文件模板的创建、应用和筛选入口；后端创建和应用接口必须再次校验启用状态。
- Agent 插件只声明受控原子操作，不注册 IIS、NGINX、RabbitMQ、Java 等产品 Handler。
- 应用资产保存插件挂载 ID、固定插件版本、普通变量、SecretRef 和证书 Artifact 输出映射。
- 部署时控制面编译 `gcac.agent-plan/v1` 签名计划；Agent 不接受未签名、过期、目标不匹配或权限越界的计划。
- Shell 默认禁用。命令只能使用 `program + args`，文件只能操作 Manifest 权限声明覆盖的路径。

Agent 插件的运行协议和原子操作规则见 `docs/项目规范/20260722-Agent插件开发规范.md`。本节只定义工作流 DSL 与 Agent 插件的协议边界，不把 Agent 插件扩展成工作流 DSL 的 step 类型。
