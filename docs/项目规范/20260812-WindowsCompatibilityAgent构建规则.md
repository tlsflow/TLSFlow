# WindowsCompatibilityAgent 构建规则（已废止）

本文件仅保留历史路径，内容不再作为项目规则执行。

自 2026-08-24 起，`agents/windows-compat-full-agent` 已迁移到 Go 1.20.x。当前唯一有效的构建和验收入口是：

`docs/项目规范/20260824-WindowsCompatibilityAgent-Go构建与验收规则.md`

旧的 C#/.NET Framework 3.5、Reference Assemblies、Windows VM、SSH 上传/回传和远端构建流程全部废止，不得继续使用，也不得恢复到发布链路。
