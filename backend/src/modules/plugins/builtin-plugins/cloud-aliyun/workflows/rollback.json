{
  "apiVersion": "gcac.workflow/v1",
  "kind": "PluginWorkflow",
  "metadata": { "name": "cloud-aliyun-certificate-rollback", "version": "2.0.1", "pluginId": "cloud.aliyun", "capability": "certificate.rollback", "readOnly": false },
  "inputContract": { "required": ["cloudServiceRef", "credential.secretRef", "certificateArtifactRef", "security"] },
  "executionBinding": { "pluginVersion": "2.0.1", "capability": "certificate.rollback", "runner": "gcac.plugin-runner/v1", "writeEffect": true },
  "steps": [{ "type": "plugin.execute", "capability": "certificate.rollback", "writeEffect": true }]
}
