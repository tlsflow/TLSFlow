{
  "apiVersion": "gcac.certificate-update-plan/v1",
  "pluginId": "web.iis",
  "capability": "certificate.rollback",
  "planId": "certificate.rollback",
  "writeEffect": true,
  "operations": [
    {
      "operationId": "rollback-iis-binding",
      "operationType": "certificate.iis.binding.rollback",
      "stage": "compensate",
      "timeoutSeconds": 300,
      "input": {
        "siteName": { "$ref": "siteName" },
        "bindingInformation": { "$ref": "bindingInformation" },
        "storeName": "My",
        "storeLocation": "LocalMachine",
        "previousThumbprint": { "$ref": "storeThumbprint" },
        "bindingKey": { "$ref": "targetId" },
        "configFingerprint": { "$ref": "configFingerprint" }
      }
    },
    {
      "operationId": "verify-rollback-iis-binding",
      "operationType": "certificate.iis.binding.verify",
      "stage": "verify",
      "timeoutSeconds": 120,
      "input": {
        "siteName": { "$ref": "siteName" },
        "bindingInformation": { "$ref": "bindingInformation" },
        "storeName": "My",
        "storeLocation": "LocalMachine",
        "expectedFingerprintSha256": { "$ref": "expectedFingerprintSha256" },
        "artifactDigest": { "$ref": "artifactDigest" },
        "bindingKey": { "$ref": "targetId" },
        "configFingerprint": { "$ref": "configFingerprint" }
      }
    }
  ]
}
